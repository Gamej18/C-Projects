#include <stdio.h>

int main (int argc, char** argv) {

    //clear_screen();
    //printf ("Running Proceses");

    //initialize IDT ~ this is down at the bottom of his instructions
    //initialize RR ~ This is the queues so dequeue and enqueue

    //retval = create_process(p1());
    // if (retval < 0)
    // {
    //     printf("error"); THERE IS PROBABALY A WAY TO LOOP THIS SO WE DON'T HAVE TO WRITE 5 IF STATEMENTS.
    // }

    //go();
}